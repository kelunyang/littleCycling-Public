# 效能審計 — plan/ 六個 demo

> 對照組是 **git HEAD**（commit `e287e58`，本輪視覺工作開始之前）。
> 全部數字由 `scripts/headless-check/` 的 probe 量出，量測條件寫在每一節。
> **這些是 CPU 端的計數，不是 GPU 實測** —— 界線見最後一節。

目標機是 **Intel N100**。已知它的兩個瓶頸不一樣：
**密集市區（台北）是 CPU/RAM 受限**（chunk 建構風暴），**稀疏路線是 fill-rate 受限**。
所以下面把「載入成本」「每幀 CPU」「填充率」分開看 —— 在 8700G 上這三件會混在一起，
而且都被硬體吃掉了。

---

## 結論

**好消息：三個 3D 世界每一項成本都大幅下降。** draw call 降到 1/3 ~ 1/5，
瓦楞紙世界的載入期貼圖成本降到 1/5，幾何記憶體降到 1/9。

**但有兩個新風險，而且都是「在 8700G 上看不出來、在 N100 上會咬人」的類型：**

1. **壓克力罩讓每個世界多了 3–5 層鋪滿畫面的半透明** —— 之前是 0 層。
   這是純填充率成本，稀疏路線正是 fill-rate 受限的場景。
2. **2D 積木世界的每幀繪圖指令變成 3.5 倍**（1344 → 4701）。
   Phaser 的 Graphics 指令是 CPU 端的，這是每一幀都要付的。

兩個都有明確的修法，見「建議」。

---

## 1. 3D 世界：前後對照

量測：`CENSUS=1 QS="?d=1400" demo-probe.mjs … --democam` ＋ `texture-probe.mjs`

| | draw calls | instances | 幾何 | 材質 | 三角形 | 屬性 MB | canvas | 貼圖像素寫入 |
|---|---|---|---|---|---|---|---|---|
| **瓦楞紙 舊** | 480 | 0 | 320 | 61 | 48,322 | 3.7 | 52 | 10.1 M |
| **瓦楞紙 新** | **266** | 740 | **70** | 75 | **17,814** | **0.4** | **35** | **1.4 M** |
| **積木 舊** | 1178 | 4663 | 198 | 189 | 57,984 | 0.4 | 3 | ~0 |
| **積木 新** | **246** | 5839 | **94** | **60** | **32,204** | 0.5 | 4 | ~0 |
| **電子 舊** | 896 | 308 | 53 | 53 | 34,399 | 0.3 | 19 | 0.8 M |
| **電子 新** | **178** | 2115 | 79 | 80 | **17,051** | 0.3 | 23 | 1.0 M |

三個世界的瓶頸本來就不同，所以優化手段也不同：

- 瓦楞紙的問題是**載入期的貼圖建構**（52 張 canvas、1000 萬次像素寫入）→ 共用一張中性
  筆觸貼圖 + `material.color` 染色 + 尺寸分級。**這一項對 N100 的「chunk 建構風暴」最直接。**
  後來把平地那層紙板換成切割墊本身，又省掉每個 chunk 的兩片側牆與兩張底板貼圖。
- 積木的問題是**材質數與三角形**（189 個材質、每顆凸點 12 段 × 3400 顆）→ 材質快取改用
  「顏色 + 序列化 opts」當 key、凸點依 chunk 距離分級（8/6/4 邊）。
- 電子的問題是**純 draw call 浪費**（896 次畫 34k 三角形）→ 建模程式碼一行不改，在
  `buildChunk` 最後把用共用幾何的 mesh 攤平進 InstancedMesh 池。

### chunk 增長：沒有失控

draw call 隨里程的變化（越遠 = 載入越多 chunk）：

| | d=400 | d=1400 | d=4000 | d=7500 |
|---|---|---|---|---|
| 瓦楞紙 舊 → 新 | 495 → **347** | 476 → **267** | 512 → **282** | 507 → **334** |
| 積木 舊 → 新 | 1243 → **282** | 1178 → **246** | 1226 → **263** | 1353 → **288** |
| 電子 舊 → 新 | 956 → **173** | 895 → **179** | 870 → **191** | 1143 → **186** |

**新版在四個里程都是平的**（±15%），沒有隨路線長度累積 —— chunk 回收是有效的。
這是「跑久了會不會越來越卡」這個問題的答案：不會。

---

## 2. 新風險 A：鋪滿畫面的半透明層

量測：`CENSUS=1` 的 `transparent draw calls … of which screen-filling`
（判準：包圍球半徑 > 300，等於鋪滿視野）

| | 透明 draw call | 其中鋪滿畫面 | 是什麼 |
|---|---|---|---|
| 瓦楞紙 舊 | 101 | **0** | — |
| 瓦楞紙 新 | 63 | **3** | 罩殼 r=940、罩壁 r=965、罩壁 r=949 |
| 積木 舊 | 23 | **0** | — |
| 積木 新 | 41 | **3** | 罩殼 r=1096、罩壁 r=980、水痕 r=1037 |
| 電子 舊 | 47 | **0** | — |
| 電子 新 | 36 | **5** | 罩殼、罩壁 ×2、另兩層 r=436 / r=319 |

**這是這輪唯一變糟的硬指標。** 壓克力罩是刻意加的視覺，但它的成本形態很特殊：
每一層鋪滿畫面的 alpha blend，在 1920×1080 是 **200 萬個 fragment**。三層就是額外
600 萬 fragment/幀。N100 的內顯只有 24 個 EU，而且**稀疏路線本來就是 fill-rate 受限**
—— 這正好打在它最弱的地方。

在 8700G 上看不出來是正常的：780M 有 12 個 CU，填充率大約是 N100 的 5–6 倍。

**這一項不需要放棄罩子，只需要讓它可關。** 見建議 1。

---

## 3. 新風險 B：2D 積木世界的每幀繪圖指令

量測：`DIST=3000 phaser-probe.mjs … --frames 400`，一幀的 Graphics 指令數

| | 舊 | 新 | 倍數 |
|---|---|---|---|
| 手繪 | 2007 | 2588 | 1.3× |
| **積木** | 1344 | **4701** | **3.5×** |
| 電子 | 1361 | 3302 | 2.4× |

積木的增量在哪，用深度層拆開就看得到：

```
舊  -100:17   -95:70    0:1231                    50:26
新  -100:16   -70:568   -10:414   0:3591  10:86   20:26
                                     ↑
                        凸點底板:1231 → 3591
```

**depth 0 那一層（有凸點的底板）從 1231 漲到 3591**，佔了整個增量的七成。
每一顆凸點都是一次 Graphics 指令，而凸點是這個世界的識別性（2D 版當初甚至為此把馬路
整條拿掉），不能砍掉。

60 fps 下 4701 指令/幀 = **每秒 28 萬次**。這是純 CPU 的工作，N100 的單核大約是
8700G 的 55%。

---

## 4. 建議（依「效益 ÷ 風險」排序）

### 1. 把壓克力罩綁進畫質分級 — 效益最大、風險最低

`packages/web/src/game/quality/graphics-quality.ts` 已經有 auto/low/med/high 分級與
FPS 調節器。罩子照著它走：

- **low**：整個關掉（3–5 層鋪滿的 alpha 全部消失）
- **medium**：只留罩壁，拿掉罩殼與高光（3 層 → 1 層）
- **high**：全開

demo 這邊已經有 `?dome=0` 可以直接量差異。**這一項單獨就能把新增的填充率成本歸零。**

### 2. 2D 凸點的視域裁切 / 分級

底板的凸點目前是整片畫。兩條路，先做前者：

- **視域裁切**：只畫 camera 可見範圍內的凸點（現在應該是整條畫）。
- **依 zoom 分級**：縮小時每兩顆畫一顆，或改畫一條帶狀底紋。

目標把 depth 0 那層拉回 2000 以下。

### 3. 瓦楞紙的貼圖成本還可以再降（選作）

37 張 canvas 裡有 8 張是剪紙樹的變體（2 種樹 × 2 種剪法 × 素/彩）。兩種剪法是為了
十字卡不要看起來像鏡射，是有理由的。但如果 N100 的載入時間仍然是痛點，這是下一個
可以動的地方（8 張 → 4 張，代價是樹的變化少一半）。

### 4. 電子世界的材質數（觀察，暫不動）

53 → 80 是這輪唯一上升的材質計數（招牌、建築燈、罩子、脈衝）。80 仍然在合理範圍
（積木世界是 60、瓦楞紙 73），但每個材質是一次 shader 編譯，是**載入期的卡頓**而非
每幀成本。若 N100 實測有進場卡頓，從這裡查。

---

## 5. 這份報告量不到的東西

**必須講清楚，不然這些數字會被當成它們不是的東西：**

- **這些是 CPU 端的計數，不是 GPU 實測。** 「鋪滿畫面的半透明層」那一節的
  fragment 數是**用包圍球半徑推估**的，不是量出來的填充率。真正的判準只有在 N100 上
  跑 frame time。
- **probe 不執行後製管線。** demo-probe 把 `WebGLRenderer` 整個換掉，所以電子世界那個
  手刻的 bloom **在這份報告裡是零覆蓋的**，它的成本沒有被計入任何一個數字。
- **三角形數是場景總量，不是實際光柵化量。** 沒有視錐裁切、沒有遮蔽剔除的模擬。
- **貼圖的像素寫入是我的 2D 光柵器算的**，跟瀏覽器實際的 canvas 成本是同一個數量級，
  但不是同一個數字。
- **2D 的 draw command 是 Phaser Graphics 的指令數**，不是 WebGL 的 draw call —— Phaser
  會把它們批次成 batch。所以那 4701 的實際 GPU 成本遠低於 4701 次，但**CPU 端建構這些
  指令的成本是實打實的**，而 N100 卡的正是 CPU。

---

## 6. 在 N100 上怎麼實際量（`?perf=1`）

`packages/web` 那套 debug-logger 在 demo 上**用不了** —— 它把紀錄批次 POST 到
`/api/debug`，需要 Fastify 後端；而 `plan/` 的六個 demo 是各自獨立的單檔 HTML，
`<script src>` 零個、沒有 server。

所以六個 demo 各自內建了一份量測，用法：**網址加 `?perf=1`**，左下角出現 HUD。

```
fps 58   p50 16.8   p95 24.1   max 33.0        ← 最近一秒
累計 62s  p95 41.3   max 128.6   >50ms 7        ← 整段(這才是判準)
calls 266  tris 17814  geo 69  tex 35           ← renderer.info(3D demo 才有)
heap 412MB  ← 點我下載 JSON
```

- 量的是 `requestAnimationFrame` 的間隔，**不跟 demo 的任何東西耦合**，所以 Three 版與
  Phaser 版共用逐字相同的一份程式碼。
- `calls` / `tris` 來自 `renderer.info`，那是**真實的 draw call**，可以用來校正本報告
  第 1 節那些由 probe 推估的數字。
- 點 HUD 會下載完整取樣的 JSON（每秒一列 + 整段的 p50/p95/p99/max）。純前端 Blob，
  不需要任何伺服器。
- 分頁切走再切回來的那個幾秒 dt 會被丟掉，不會汙染 max。

**顯示的是累計的尾巴，不是最後一秒**：N100 不是「一直慢」，是「好好的然後掉一幀
120 ms」。60 幀取平均會把那一幀稀釋成 +1.4 ms，騎的人卻感覺得到。
`avg 16 / max 130` 跟 `avg 30 / max 34` 是完全不同的問題。

`packages/web` 的每秒 perf 紀錄也補上了同樣的四個欄位
（`p50FrameMs` / `p95FrameMs` / `maxFrameMs` / `longFrames`），所以 demo 與正式遊戲
兩邊的 jsonl 可以直接對照。

### 要跑的實驗

| # | 網址 | 回答什麼 |
|---|---|---|
| 1 | `?d=7500&perf=1` | 基準：載入最多 chunk 時的 p95 與 >50ms 次數 |
| 2 | `?d=7500&perf=1&dome=0` | **壓克力罩的真實代價**（跟 1 的差） |
| 3 | `?d=7500&perf=1&night=1&rain=1` | 最壞情況（夜 + 雨 + 全部粒子） |

每個跑滿一分鐘再點下載。三份 JSON 就能回答「N100 撐不撐得住、以及該砍誰」。
